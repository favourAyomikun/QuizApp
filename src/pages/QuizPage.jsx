import React from 'react'

const QuizPage = (props) => {
  const test = props.inputval
  console.log(test);
  return (
    <section className=''>
      {/* <h1>Welcome {test}</h1> */}
    </section>
  )
}

export default QuizPage;